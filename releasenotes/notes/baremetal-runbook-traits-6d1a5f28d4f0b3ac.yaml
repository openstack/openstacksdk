---
features:
  - |
    Adds support for bare metal API microversion 1.112, which adds a
    ``description`` field and a ``traits`` field to runbooks. Runbook traits
    are managed through the new ``add_runbook_trait``,
    ``remove_runbook_trait`` and ``set_runbook_traits`` proxy methods.
